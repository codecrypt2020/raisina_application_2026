import 'package:flutter/material.dart';

class NetworkMain extends StatelessWidget {
  const NetworkMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
